<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN"
  "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<!--
PrivacyFence daemon LaunchAgent — auto-starts at login.

Install:
  cp com.privacyfence.app.plist ~/Library/LaunchAgents/
  launchctl load ~/Library/LaunchAgents/com.privacyfence.app.plist

Uninstall:
  launchctl unload ~/Library/LaunchAgents/com.privacyfence.app.plist
  rm ~/Library/LaunchAgents/com.privacyfence.app.plist

The WorkingDirectory should point to your privacyfence project root so that
config/settings.yaml is resolved correctly. Adjust ProgramArguments to the
absolute path of your privacyfence-app script (inside the project venv).

Note that this is a LaunchAgent: it runs the daemon as whoever is logged in,
which is also the account the AI client PrivacyFence governs runs as. #428
Phase 4 offers the alternative -- scripts/macos_privilege_separation.sh
installs a LaunchDaemon under a dedicated account instead
(installer/macos/com.privacyfence.daemon.plist.tmpl), plus a LaunchAgent for
the companion app, and disables this file if it finds it installed. See
docs/security-and-compliance.md's "Privilege separation (macOS, opt-in)".
-->
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.privacyfence.app</string>

  <key>ProgramArguments</key>
  <array>
    <!-- Replace with the absolute path to privacyfence-app in your venv. -->
    <string>/Users/andrastakacs.private/Coding/privacyfence/.venv/bin/privacyfence-app</string>
  </array>

  <key>WorkingDirectory</key>
  <string>/Users/andrastakacs.private/Coding/privacyfence</string>

  <key>RunAtLoad</key>
  <true/>

  <!-- Restart automatically if the daemon exits unexpectedly. -->
  <key>KeepAlive</key>
  <dict>
    <key>SuccessfulExit</key>
    <false/>
  </dict>

  <key>StandardOutPath</key>
  <string>/Users/andrastakacs.private/Coding/privacyfence/logs/privacyfence-daemon.log</string>

  <key>StandardErrorPath</key>
  <string>/Users/andrastakacs.private/Coding/privacyfence/logs/privacyfence-daemon.log</string>

  <!-- Give the UI time to start before LaunchAgent considers it hung. -->
  <key>ThrottleInterval</key>
  <integer>5</integer>
</dict>
</plist>

# PrivacyFence configuration example.
# Copy this file to config/settings.yaml and adjust as needed.
#
# This file is USER-level only — privacy policy, which connectors are
# enabled, and auto-accept rules. It carries no secrets:
#
#   - Organization-level app credentials (the Google OAuth client, Slack app,
#     Telegram app, Salesforce Connected App, Atlassian OAuth app) are
#     installed as one bundle via "Install/Update Organization Config…" in
#     PrivacyFence Settings (the embedded web page, web.settings below). IT
#     admins build that bundle once with scripts/build_org_bundle.py and
#     distribute it.
#   - Per-user sign-in (browser OAuth, or Telegram's phone + code login) is
#     done via "Authenticate…" per connector, also in PrivacyFence Settings.
#
# Never commit config/settings.yaml or any file under credentials/.

# All six category-schema groups below (privacy / drive_privacy /
# slack_privacy / contacts_privacy / tasks_privacy / confluence_privacy) are
# also editable live from PrivacyFence Settings' "Privacy Filter" page (no
# restart needed) -- editing them here or from that page are equivalent.
privacy:
  # Policy applied when a category is not explicitly configured below.
  # One of: allow, redact, block
  default_policy: block

  # Per-category filtering applied BEFORE the user sees the data in the review UI.
  # The privacy filter is a floor (a minimum protection), not a ceiling -
  # the user still has to approve every request.
  #   allow  -> data passes through unchanged
  #   redact -> data is replaced by a placeholder revealing only its length
  #             (e.g. "[REDACTED BY PRIVACY FILTER — 42 characters withheld]") -
  #             a full replacement, not a partial reveal of the content;
  #             for list-shaped categories this behaves the same as block
  #   block  -> data is replaced with "[BLOCKED BY PRIVACY FILTER]"
  categories:
    body: allow
    metadata: allow        # sender / recipients / date / subject
    attachments: block     # attachment metadata; downloading content is a separate, approval-gated tool
    thread_history: allow

drive_privacy:
  # Also covers Google Sheets (drive_sheets_* tools) - Sheets is not a
  # separate connector, it rides on the Drive connector's OAuth grant.
  default_policy: block
  categories:
    file_content: allow      # the actual document text / bytes (highest risk)
    file_metadata: allow     # name / owners / created/modified times / sharing
    file_list: allow         # results from list operations (names + ids)
    folder_structure: allow  # folder listing results
  # NOTE: file_metadata and file_list cover overlapping fields (a file's name/
  # owners) but gate two different tools (drive_get_file_metadata vs.
  # drive_list_files) independently - restricting one without the other
  # still lets that same information through the tool whose category is
  # still "allow". PrivacyFence logs a startup warning if they're set
  # inconsistently (privacy_filter.check_consistency_warnings).

slack_privacy:
  default_policy: block
  categories:
    message_content: allow   # the text of messages
    user_identity: allow     # user names, emails, real names
    channel_list: allow      # channel names and metadata
    thread_content: allow    # thread reply content
    dm_list: allow           # 1:1 DM listing (who you DM with)
    group_chat_list: allow   # group-DM listing (id, name, participants)

contacts_privacy:
  default_policy: block
  categories:
    notes: block   # free-text biography field - the one field here that can
                   # carry arbitrary personal content, unlike name/email/
                   # phone/organization/job_title, which have no category of
                   # their own and always pass through unfiltered

tasks_privacy:
  default_policy: block
  categories:
    notes: block   # free-text notes field - same reasoning as
                   # contacts_privacy.notes; title/due/status have no
                   # category of their own

confluence_privacy:
  default_policy: block
  categories:
    search_excerpt: block   # the content excerpt confluence_search/
                            # confluence_cql_search return alongside each
                            # match - the one place Confluence's otherwise
                            # metadata-only auto search tools return actual
                            # page content pre-approval
    attachments: block      # attachment metadata; downloading content is a separate, approval-gated tool

calendar:
  # Whether calendar_get_free_busy returns full event titles/times/status for
  # a queried colleague when the authenticated account already has calendar
  # access to see them (falls back to busy/free-only automatically when
  # access isn't available either way). Set to false to always return
  # busy/free blocks only, regardless of access - a more conservative choice
  # for scheduling queries across a team, at the cost of losing event
  # titles/status in the (auto-approved) free/busy result.
  free_busy_full_event_details: true

# Enable/disable individual connectors (all enabled by default — this section
# is only needed to turn one off). Toggled automatically when you use
# Disable/Enable in PrivacyFence Settings. Enabling here isn't enough on its
# own: a connector also needs its organization config installed AND to be
# authenticated (both from PrivacyFence Settings) before it actually runs.
# connectors:
#   telegram: { enabled: false }

pii_detection:
  # Extra confirmation gate, on top of the normal Allow once/Deny popup:
  # scans the message/document/spreadsheet content shown in the approval
  # popup for likely personal data (Hungarian, English, and German — IBANs,
  # credit card numbers, national ID/tax/social-security numbers, and
  # similar; email addresses and phone numbers are deliberately not
  # detected, see docs/TECHNICAL_REFERENCE.md). When something is flagged,
  # the popup is tinted red and a second "Are you sure?" dialog is required
  # before Allow once (or Always allow) takes effect. Toggled automatically
  # when you use "PII Detection Gate" in PrivacyFence Settings.
  enabled: true

  # IP addresses and currency figures show up constantly in ordinary
  # business correspondence (server logs, invoices, budgets) without being
  # personal data about anyone, so these two categories can be turned off
  # independently of the gate as a whole. Toggled automatically from the
  # "PII Detection Gate" section of PrivacyFence Settings.
  detect_ip_addresses: true
  detect_financial_figures: true

  # PII-refinement trial capture -- OFF by default, and not meant to stay on
  # indefinitely. When true, the audit log's "PII Match Details" column also
  # records the literal matched text (redacted for value-bearing categories
  # like IBAN/credit card/national IDs -- see docs/TECHNICAL_REFERENCE.md's
  # "PII detection gate" section) for a request that was actually approved,
  # or a fixed "details hidden" placeholder for one that was denied. There's
  # no Settings-page toggle for this one deliberately -- set it for a bounded
  # trial window (restart required to pick it up), review the weekly Excel
  # export's "By PII category" breakdown to see which patterns are noisy,
  # then turn it back off.
  audit_match_details: false

update_check:
  # Once-a-day background check against GitHub Releases
  # (https://github.com/privacyfence/privacyfence/releases) for a newer version. Never downloads
  # or installs anything automatically -- only shows an in-page banner with a link to the release
  # page. A network failure here is silent (logged, not shown) and never blocks startup. Toggled
  # automatically when you use "Check for Updates" > "Enabled" in PrivacyFence Settings.
  enabled: true

  # Off by default. When on, the daily check also considers pre-release (beta/rc) tags, not just
  # the latest stable release -- opt in here if you're a beta tester. Toggled automatically when
  # you use "Check for Updates" > "Receive Beta Releases" in PrivacyFence Settings.
  include_beta: false

# Note: unattended_sessions.enabled (privacyfence_begin_unattended_session's
# opt-in gate) is NOT set here. It's a deliberate per-organization choice,
# not a per-user setting, so it lives in the organization config bundle
# (org_config.json, installed via "Install/Update Organization Config…" in
# PrivacyFence Settings) — see docs/TECHNICAL_REFERENCE.md's "Scheduled /
# unattended Cowork tasks" section.

# Trust a specific resource once — a folder, a task list, a channel, ... —
# instead of adding the same ID to several operation keys under
# auto_accept_rules below. Each entry is grouped by connector and resource
# type, with a small set of capability booleans (read / write / create /
# ...) that decide which operations it actually auto-accepts; every new
# grant starts with every capability off, so adding one does nothing until
# you explicitly flip a capability on. This is also the section
# PrivacyFence Settings' "Auto-accept Rules → <Connector>" page reads and
# writes — editing it here or from that page are equivalent, and ``name``
# is a cosmetic label (last resolved from the connector, shown in the page)
# that the evaluator itself never reads. See "Auto-accept grants" in
# docs/TECHNICAL_REFERENCE.md for the full per-connector reference.
# auto_accept_grants:
#   drive:
#     sandbox_folders:
#       # A folder Claude can write into without a popup each time (e.g. a
#       # scratch space you created for it) — also covers Sheets/Docs writes
#       # (write_range/add_sheet/rename_sheet/format_range/insert_dimensions/
#       # delete_dimensions/docs.edit_content/docs.format_content) in that
#       # folder.
#       - id: "<drive folder id>"
#         name: "Claude scratch space"
#         write: true
#     folders:
#       # A folder Claude can read from without a popup (does NOT also grant
#       # write — that's the separate "sandbox_folders" list above).
#       - id: "<drive folder id>"
#         name: "Shared Reports"
#         read: true
#   tasks:
#     task_lists:
#       - id: "<task list id>"
#         name: "Personal"
#         create: true
#         edit: true
#         complete: true
#         move: true

auto_accept_rules:
  contacts.edit:
    # Auto-accept contact edits that don't touch emails/phones (name,
    # organization, or notes-only changes)
    - rule: no_contact_info_change
  # sheets.write_range:
    # Auto-accept writes to spreadsheets created earlier in the same session
    # (e.g. filling in a sheet PrivacyFence just created for you)
    # - rule: created_this_session
  # gmail.create_label:
    # Auto-accept creating labels whose name is in the allowlist (nested
    # names match on the full "Parent/Child" string)
    # - rule: label_name_allowlist
    #   value: ["Receipts", "Travel/Bookings"]
  # Trusting a specific folder, task list, channel, project, space, chat,
  # calendar, or Salesforce report? Use auto_accept_grants
  # above instead — it's the same underlying mechanism (this is what it
  # compiles down to) but configured once per resource instead of once per
  # operation key.

logging:
  # Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
  level: INFO
  # Log file path. Relative paths are resolved against the project root.
  file: logs/privacyfence.log

web:
  # Port the embedded HTTP server listens on -- shared by the approval
  # surface, /mcp below, and /settings, one server either way. Always
  # bound to localhost only in this (local) mode -- see web/server.py's
  # module docstring. The embedded server itself always runs in local mode
  # since P10, decision D6: the web approval UI is the only one there is,
  # so there's no "off" switch for it the way there used to be a
  # native/web choice (`web.approval_ui`, removed at P10 -- this key is
  # simply gone now, not just defaulted).
  port: 8765
  mcp:
    # Turns on the /mcp Streamable HTTP endpoint (P2) -- Claude talks to
    # PrivacyFence directly over HTTP. On by default as of D11/P4b:
    # PrivacyFence.mcpb installs a thin stdio shim for Claude Desktop that
    # only speaks /mcp, so a fresh install needs this on for that
    # extension's zero-config promise ("no config file edited") to
    # actually hold -- P4b's own exit criterion. Since P5 retired the
    # original bridge/IPC-socket transport, this is no longer "additive
    # alongside the bridge" -- it's the only way Claude reaches this
    # daemon at all. Flip to false only if you want this daemon completely
    # unreachable from Claude for a while (a full stop, not a fallback to
    # something else that still works). Once enabled, point a Streamable
    # HTTP MCP client at http://localhost:<port>/mcp with
    # `Authorization: Bearer <the token in ~/.privacyfence/mcp_token>`.
    enabled: true
  settings:
    # Serve /settings and its POST /api/settings/{action} dispatcher.
    # Through P9 this defaulted to false, since a native settings window
    # covered the same ground; P10 deleted that window (decision D6), so
    # this now defaults to true -- without it there is no interactive way
    # at all to install an organization config bundle, authenticate a
    # connector, or edit a rule (hand-editing this file and
    # org_config.json directly still works regardless of this setting).
    # Flip to false only if you're deliberately editing config by hand and
    # want /settings unreachable too.
    enabled: true
    # The web settings page's "Quit PrivacyFence" action stops the server
    # rendering the page it was clicked on -- confirmed in-page either way,
    # but this is the local-mode-only kill switch for the action entirely.
    # Will be refused outright once `mode: org` exists (a future phase);
    # local mode leans "the token is enough" for now.
    allow_quit: true
  notifications:
    # Desktop notifications for a new pending approval while the
    # /approvals tab is open but not focused (docs/approval-list-ui-ux.md
    # §4) -- tiers 0 (title badge, aria-live announcement) and 1
    # (registration.showNotification(), no push service involved, nothing
    # leaves the machine) only. Tier 2 (web push, a closed tab, a phone) is
    # org mode/P7+ work and has no key here yet.
    enabled: true
    # How much a tier-1 notification is allowed to say, when it's for
    # exactly one new pending approval (grouped/multi-approval
    # notifications always stay at the `minimal` count-only body -- see
    # web_shell.py's notificationBody()):
    #   minimal   "1 approval pending" -- never anything else
    #   standard  adds connector + tool + direction, e.g. "Gmail — Read
    #             Email · read" -- fixed, developer-authored copy, never
    #             gated content
    #   detailed  adds the row's own title/summary line too (e.g. "Send
    #             email to alice@example.com") -- the one level that can
    #             put gated content on a lock screen/watch/meeting-room
    #             mirror; never the default (docs/approval-list-ui-ux.md
    #             §4.3)
    detail: standard # minimal | standard | detailed

# WebAuthn passkey step-up (#426 Phase 1/2) -- enrolling a passkey here
# mounts /security (linked from the Settings page) and lets you register a
# Face ID/Touch ID/Windows Hello credential against this install.
# Enrolled credentials live under a service-owned subdirectory of the data
# directory, not here.
#
# `enabled` and `require_passkey` are deliberately left unset below rather
# than written out as `false`. Unset means "whatever this install can
# actually back": ON for a packaged install (the DMG/.pkg, the Windows
# installer, the .deb), which is privilege-separated or refuses to serve at
# all, so the credential store a passkey is checked against is out of the
# agent's reach; OFF for a source checkout or a `pip`/`pipx install`, where
# nothing separates anything and a passkey checked against a store the
# agent can write is a checkbox a local process ticks for itself. Write
# either key out by hand to decide for yourself -- an explicit value always
# wins, in both directions.
step_up:
  # Turns on step-up at decide time: releasing an approving decision
  # (accept/accept_all) on a gated write -- or, with the wider scope below,
  # a PII-flagged read -- needs a fresh passkey assertion. `deny` never
  # needs one. This is still opt-in machinery, not a guarantee you can rely
  # on by itself: with nothing enrolled, there's no ceremony to demand and
  # the decision goes through unguarded -- so this only does something once
  # you've actually added a passkey at /security. Making enrollment
  # mandatory is `require_passkey` below.
  #
  # You don't have to hand-edit this file to turn it on: once a passkey is
  # enrolled at /security, the Settings page's Security card has a "Turn
  # on" button that sets `enabled` and `require_passkey` together (B9 of
  # the 4.1.0 action plan) and takes effect immediately, no restart. Editing
  # this file directly still works too, and is the only way back to
  # `false` -- there's no UI control that turns step-up back off.
  #
  # Unset: on for a packaged install, off otherwise (see the note above
  # this section). Uncomment to decide for yourself.
  # enabled: false
  # Which approvals require step-up, narrowest first:
  #   writes                 every approval popup for a write
  #   writes_and_pii_reads   also a read PrivacyFence flagged as containing
  #                          personal data
  #   writes_and_reads       also every other read that reaches an approval
  #                          -- pick this if you'd rather not depend on PII
  #                          detection having flagged what matters
  # A read that an auto-accept rule already covers never becomes an approval
  # at all, so no scope here asks for a passkey on one. Changing this value
  # needs a daemon restart (the Settings page's "Turn on" button writes
  # `enabled`/`require_passkey` only).
  #
  # Unset means `writes_and_pii_reads`: the widest scope that only ever
  # asks about something PrivacyFence has an actual reason to think is
  # sensitive. Uncomment for the narrower `writes`, or widen to
  # `writes_and_reads` if you'd rather not depend on PII detection at all.
  # scope: writes_and_pii_reads
  # WebAuthn's Relying Party ID. Leave unset -- "localhost" is always
  # correct for this daemon's own embedded server and needs no TLS.
  rp_id: localhost
  # Shown to your platform authenticator's own passkey picker/prompt.
  rp_name: PrivacyFence
  # Whether a passkey assertion is *mandatory*, not merely offered when one
  # happens to be enrolled (#426 Phase 3). With this on: an approving
  # decision with nothing enrolled is refused outright (a 403 naming
  # /security), not silently released; the same is true for a sensitive
  # subset of the settings actions (rule/grant/policy/PII changes -- see
  # web/routes_settings.py's own _SENSITIVE_ACTIONS); and removing your
  # last enrolled credential always needs a fresh assertion first,
  # regardless of this flag. If nothing is enrolled when the daemon starts
  # with this on, it still starts -- refusing to boot would remove the one
  # path (/security) that fixes it -- but every page carries a persistent
  # banner until a passkey is added, and no approving decision is released
  # in the meantime. On a packaged install the companion app walks you
  # through enrolling the first one at its next start, so that state is
  # meant to last minutes rather than to be lived in.
  #
  # Setting this to `true` on an install that is *not* privilege-separated
  # (ADR 0003) is refused outright at startup, naming the fix: this flag
  # and the enrolled-credential store would both be files the agent this
  # check defends against can edit, which makes the guarantee worse than
  # not having it at all.
  #
  # Unset: on for a packaged install, off otherwise (see the note above
  # this section). Uncomment to decide for yourself.
  # require_passkey: false
  # The approval binder's own batch decide endpoint (/approvals, Phase 3):
  # "single_assertion" -- the default -- lets one passkey prompt release a
  # whole selected batch of approvals at once, bound to the exact set you
  # selected (adding, dropping, or flipping any one item invalidates it).
  # "per_item" is the stricter alternative for an install that never wants
  # one prompt to cover more than one decision: with it set, the batch
  # endpoint refuses outright to release anything in the batch that needs
  # step-up (nothing in the batch is applied), so those have to be decided
  # one at a time from their own card instead. A deny-only batch is
  # unaffected either way -- denying never needs step-up.
  batch: single_assertion

# P3 of the auto-accept policy redesign: which evaluator decides an auto-accept call. Both the
# existing evaluator and the new policy/engine.py one run on every call regardless of this
# setting -- this only decides which answer is acted on, and which is shadowed (logged at
# WARNING on a disagreement, never at the content level). Leave unset, or "v1", to keep today's
# evaluator load-bearing; this is the default for a reason -- nothing on disk changes yet, and no
# auto-accept rule you've configured needs touching for this release. Setting "v2" makes the new
# evaluator authoritative instead; switching back to "v1" is the documented rollback if that
# regresses something, no release needed.
policy:
  engine: v1

# ADR 0007 (docs/adr/0007-local-file-bridge.md): the local file bridge --
# how a privilege-separated daemon moves bytes to/from a path an agent
# named (drive_upload_file's local_path, drive_download_file's
# destination_dir, the Gmail/Confluence attachment tools) through the
# .mcpb shim instead of reading/writing the user's filesystem directly.
# Unset means every default below applies -- most installs never need this
# section at all.
file_bridge:
  # A download this large is held fully in memory before it's staged for
  # the shim (or, with no shim, before a one-time link is handed out) --
  # not streamed to disk on this side. 200MB is generous for anything a
  # Drive/Gmail/Confluence tool plausibly downloads; raise it only if you
  # know this machine has memory to spare for occasional larger files.
  # Uncomment to change it.
  # max_download_bytes: 200000000

{
  "name": "privacyfence-mcpb-shim",
  "version": "0.0.0-dev",
  "description": "PrivacyFence .mcpb shim: a thin stdio-to-Streamable-HTTP transport proxy Claude Desktop spawns, forwarding MCP frames between its own stdio and the PrivacyFence daemon's /mcp endpoint. Carries no PrivacyFence protocol knowledge -- see src/index.ts's module docstring.",
  "private": true,
  "type": "module",
  "license": "Apache-2.0",
  "scripts": {
    "build": "node build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0"
  },
  "devDependencies": {
    "@types/node": "26.4.1",
    "esbuild": "0.28.2",
    "tsx": "4.23.13",
    "typescript": "7.0.2"
  }
}
